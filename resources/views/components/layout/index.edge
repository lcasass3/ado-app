<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ $props.get('title') || 'AdoApp' }}</title>

    @if($slots.meta) 
      {{{ await $slots.meta() }}}
    @end

    @vite(['resources/css/app.css', 'resources/js/app.js'])
  </head>
  
  <body class="bg-background min-h-screen w-screen">
      {{{ await $slots.main() }}}
  </body>
</html>